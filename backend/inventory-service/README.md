# Inventory Service

Source of truth for products and stock. Prevents oversell with atomic DB updates.

## Run (local)
```bash
npm install
npm run build
npm run start:dev
```

## Env
See `.env.example`.

## gRPC
Defined in `proto/inventory.proto`.
