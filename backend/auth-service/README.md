# Auth Service

Handles user registration, login, token validation, and logout (token revocation).

## Run (local)
```bash
npm install
npm run build
npm run start:dev
```

## Env
See `.env.example`.

## gRPC
Defined in `proto/auth.proto`.
